package test;

import static org.junit.Assert.*;

import java.io.File;

import primeministers.Reader;

import org.junit.Test;

public class ReaderTest {

	@Test
	public void test() {
		/*
		primeministers.Reader reader = new primeministers.Reader(new File("/Users/sueSama/GitHub/PrimeMinisters/PrimeMinistersByJava/PrimeMinisters.csv"));
		primeministers.Table aTable = reader.table();
		System.out.println(aTable.tuples().toString());
		assertNotNull(aTable);
		*/
	}
}
